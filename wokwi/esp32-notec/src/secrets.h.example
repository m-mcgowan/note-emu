// Copy this file to secrets.h and fill in your values.
// secrets.h is gitignored and will not be committed.

#define WIFI_SSID "Wokwi-GUEST"
#define WIFI_PASS ""
#define NOTEHUB_PAT "your-notehub-api-token"

// Optional — uncomment and set to your Notehub ProductUID (reverse-DNS, e.g.
// "com.your-co.you:my-project") to bind this virtual Notecard to a project.
// Without it the device runs fine but won't appear in any Notehub project's
// Devices tab; you can still bind it manually via {"req":"hub.set",...}.
// #define NOTEHUB_PRODUCT "com.your-co.you:my-project"
