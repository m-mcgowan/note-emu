// Copy this file to secrets.h and fill in your Notehub PAT.
// secrets.h is gitignored and will not be committed.
//
// The WiFi defaults below are the Wokwi virtual network; leave them as
// they are when running under Wokwi (browser sim or VS Code extension).
// For real hardware, override with your own WIFI_SSID / WIFI_PASS.

#define WIFI_SSID "Wokwi-GUEST"
#define WIFI_PASS ""
#define NOTEHUB_PAT "your-notehub-api-token"
